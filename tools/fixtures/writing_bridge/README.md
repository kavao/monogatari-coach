# writing_bridge fixtures

`SCHEMA.md` が Phase 0 の契約正本です。このフォルダの YAML / JSON は中立語彙の例であり、実装コードではありません。

作品本文や 106 のイベントは変更していません。`ok_ch01_001/work/` は架空の作品ルートです。

ハッシュを本文に追従させるときだけ `_emit_run_artifacts.py` を使う。runtime の CLI ではない。
