[tool.pyright]
# PyYAML は実行時に問題なく import できるが、環境によっては「ソースから解決できない」
# （reportMissingModuleSource）が誤検知される。デバッガではなく静的解析の設定。
reportMissingModuleSource = false

[tool.basedpyright]
reportMissingModuleSource = false
